{
  "name": "chrome-extention-json-viewer-jq",
  "version": "1.0.0",
  "description": "JSON  viewer + jq filter",
  "main": "index.js",
  "scripts": {
		"build": "tsc && cp manifest.json build && cp -r src/assets/ build",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "typescript": "^5.3.3"
  }
}
